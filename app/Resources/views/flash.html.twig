{% for type, messages in app.session.flashBag.all %}
    <div class="alert alert-{{ type }}">
        {% for message in messages %}
            <span class="text-{{ type }}">{{ message }}</span><br>
        {% endfor %}
    </div>
{% endfor %}